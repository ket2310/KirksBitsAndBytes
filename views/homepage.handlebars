<div class="menu-container">
   <div class="card-header col-md-10">
      {{#each blogs }}
         <a href="/blog/{{id}}">{{title}}</a>
         <p>{{content}}</p>
         <p>Created by {{poster.name}} on {{format_date date_posted}}</p>
            {{#each comments as |comment|}}
               <p><span>{{comments}}</span></p>
            {{/each}}
      {{/each}}
  </div>
</div>